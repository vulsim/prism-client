<UserControl x:Class="Prism.Visual.Controls.FuseButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>       
        <Button Name="button" Click="button_Click">
            <Grid>
                <TextBlock Name="textBlock" Text="{Binding Text}"/>
            </Grid>            
        </Button>
        <Grid Background="#22007ACF" Name="overlay" Visibility="Hidden" IsHitTestVisible="False" />
    </Grid>
</UserControl>
